{
  "name": "tunnel-server",
  "version": "1.0.0",
  "description": "VPS tunnel server - accepts WebSocket connections and proxies HTTP traffic",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
